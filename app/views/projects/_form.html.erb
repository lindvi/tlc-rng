<%= form_for(@project) do |f| %>
	<div class="field">
	    <%= f.label :project_name %><br />
	    <%= f.text_field :project_name %>

	    <%= f.label :project_size %><br />
	    <%= f.number_field :project_size %>
	</div>
	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>